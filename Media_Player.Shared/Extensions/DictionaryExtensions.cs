using System;
using System.Collections.Generic;
using System.Text;

namespace Media_Player.Extensions
{
    public static class DictionaryExtensions
    {
        /*public static bool TryGetKeyFromValue<TKey, TValue>(this Dictionary<TKey, TValue> dictionary, TValue value, out TKey key)
        {
            foreach (var item in dictionary)
            {
                if (item.Value == value)
                {

                }
            }

            return false;
        }*/
    }
}
